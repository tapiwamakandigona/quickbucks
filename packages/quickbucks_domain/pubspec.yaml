name: quickbucks_domain
description: Pure-Dart business logic for QuickBucks (money, dates, loans, share-out). No Flutter imports.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.5.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
